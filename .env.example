# Copy this file to .env and fill in real values. Never commit .env to git.

# Where the Contact page form submits leads (JSON POST).
# This should be YOUR OWN backend/CRM endpoint — never a third party's domain.
# If that endpoint requires an API key, add the key server-side (in your backend),
# not here — anything in a VITE_ variable is bundled into the public JS and visible
# to anyone who views the site's source.
VITE_LEADS_API_URL=https://your-backend.example.com/api/leads

# Cashfree payment backend — creates orders server-side (never expose secret keys in frontend).
# Your backend should call Cashfree Create Order API and return { orderId, paymentSessionId }.
VITE_PAYMENT_API_URL=https://your-backend.example.com/api/payments/create-order

# Cashfree mode: "sandbox" for testing, "production" for live payments
VITE_CASHFREE_MODE=sandbox
